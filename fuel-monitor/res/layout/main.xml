<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <Button
        android:id="@+id/fuelingButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="40dp"
        android:layout_marginTop="80dp"
        android:background="@null"
        android:drawableTop="@drawable/fueling"
        android:text="@string/menu_addfueling"
        android:textColor="@android:color/primary_text_dark"/>

    <Button
        android:id="@+id/vehiclesButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/fuelingButton"
        android:layout_marginLeft="65dp"
        android:layout_toRightOf="@id/fuelingButton"
        android:background="@null"
        android:drawableTop="@drawable/car"
        android:text="@string/menu_vehicles"
        android:textColor="@android:color/primary_text_dark"/>

    <Button
        android:id="@+id/statsButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/fuelingButton"
        android:layout_below="@+id/fuelingButton"
        android:layout_marginTop="40dp"
        android:background="@null"
        android:drawableTop="@drawable/stats"
        android:text="@string/menu_stats"
        android:textColor="@android:color/primary_text_dark"/>

    <Button
        android:id="@+id/gasButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/statsButton"
        android:layout_alignBottom="@+id/statsButton"
        android:layout_alignLeft="@+id/vehiclesButton"
        android:background="@null"
        android:drawableTop="@drawable/gas_station"
        android:text="@string/menu_fuelstations"
        android:textColor="@android:color/primary_text_dark"/>

</RelativeLayout>